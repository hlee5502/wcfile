#include <stdio.h>
#include <stdlib.h>

#ifndef CH_HEAD
#define CH_HEAD

// solve function reads equations from a file and writes them in a solved state to another file

// param qfile: file with math questions to solve
// param afile: file to write equations with answers in

void solve(const char* qfile, const char* afile);

#endif
